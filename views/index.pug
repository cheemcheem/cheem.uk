extends layout

block content
    div#header
        h2#title List of PCS
        #current
            .current-info
                h3 Selected PC Name 
                span#current-pc N/A
            .current-info
                h3 Selected PC Users
                span#current-users N/A
        div#filter
            input#search(placeholder="  Search by pc or user name")
            select#labSelector
                optgroup(label="John Honey")
                    option(value="JH105") JH105 Quiet
                    option(value="JH103") JH103 Goldfish
                    option(value="JH110") JH110 Main
                    option(value="Morrison") Morrison
                optgroup(label="Jack Cole")
                    option(value="JC035") JC035 Main
                optgroup(label="List")
                    option(value="List") List of PCs
    div#content
        div#views
            svg.map#JC035
            svg.map#JH105.visible
            svg.map#JH110
            svg.map#JH103
            svg.map#Morrison
            table#List
                thead
                    tr
                        th Address
                        th Users
                tbody#pcTableBody
                    each pc in pcList
                        tr.pc
                            td.address #{pc}
                            td.users


    script(src="javascripts/script.js")


